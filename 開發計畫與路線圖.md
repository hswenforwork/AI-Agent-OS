# 開發計畫與路線圖

依據 2026-09-14 grill-me 訪談（Claude Cowork，紀錄在使用者的 Claude Project「AI分身」`brainstorms/2026-09-14-ai-agent-os-revamp-plan.md`）整理。這份文件回答兩個問題：這一版做了什麼、下一步還缺什麼。

## 訪談確認的方向（背景）

- 目標不是一個展示型儀表板，而是 Claude、ChatGPT、Gemini 等不同模型的代理真的共同負責一個專案、任務目標一致、資訊互通、使用者能集中管控。
- 交接方式選擇「做法 A：自動化」——平台直接用各家 API 呼叫，不是使用者手動在不同網頁間複製貼上。
- 預算：每月抓 500–1000 元新台幣試試看，對三個供應商來說偏緊，節流是硬約束不是事後優化。
- 監督模式：分階段確認制——跑到檢查點就停下，給使用者看結果與花費，核准才繼續，不做完全無人值守。
- 這個平台本身被排進使用者本季（未來 90 天）第三項優先事項，跟台股風險工具、3D 治具工具並列。
- 分工：依任務類型動態分派，使用者在平台上手動選模型，不是系統自動判斷、也不是固定角色。
- 品保：保留交叉檢查但可彈性跳過，跳過與否由使用者針對每個任務決定。
- 第一個試跑任務：拿平台自己當第一個任務——由這個 Claude session 先把地基搭出來，之後才輪到「平台管理自己接下來的開發」。

## Phase 0（本次已完成，2026-09-14）

這一版是「打地基」階段，用現有已證實可行的方式（Claude session 直接寫程式、commit、push）完成，還不是靠平台自己的自動化機制完成的——這是訪談裡已經指出的必經順序（系統還沒蓋好，不能用系統蓋自己）。

已完成：

1. **多供應商工作台**：`系統設定/AI服務設定.json` 改成 Gemini／OpenAI／Claude 三家並列，AI 工作台可以針對每一次任務手動挑選供應商與模型（對應「依任務類型動態分派」的決定），三家金鑰分開輸入、都只暫存在分頁記憶體。
2. **分階段確認制**：「啟動全隊」不再一次跑完六個階段，改成每完成一階段就暫停，顯示這階段的結果與預估花費，使用者按「核准並繼續」才會進到下一階段，隨時可以按「停止」。
3. **品保可彈性跳過**：新增「略過品保檢查」勾選框，勾選後團隊流程會跳過檢查代理這一階段。
4. **費用預估與月預算**：依文字長度粗略估算每次呼叫的花費，累計本月花費並對照使用者可調整的月預算（預設 NT$800，落在訪談確認的 500–1000 區間中間），接近或超過預算會有顏色提示，送出前若會讓當月累計超過預算會跳出確認視窗（陽春版的「自動煞車」）。
5. **共用錯誤記憶**：新增 `知識資料庫/已知問題與規則.md`，加進所有代理的知識文件清單；`AI代理/檢查代理.md` 明訂檢查代理必須把確認過的真實問題寫進這份文件，其他代理動手前也要先看過。這是直接針對使用者 top_pain（工具改壞了不知道）設計的機制。
6. 更新 README、平台操作說明、資料格式說明、新增多模型 API 串接說明，反映以上所有變更。

尚未完成、刻意留給下一階段：

- 三個模型的 API 呼叫都還是「瀏覽器直接連線＋使用者每次貼金鑰」，還沒有真正的伺服器端金鑰保管與背景自動執行。
- 費用估算是文字長度粗略換算，不是供應商即時用量 API；Gemini 目前的呼叫方式沒有回傳真正的 token 使用量。
- 「啟動全隊」目前一次只能套用一個供應商／模型（開始團隊時選定），還沒辦法讓六個階段各自指定不同供應商；要混用不同模型，目前要靠「交給此代理」單一代理模式一個一個手動切換。
- 月預算的「自動煞車」只在使用者按下送出的當下用 `confirm()` 詢問，不是真正在背景自動擋下超支的呼叫。

## Phase 1（下一階段，尚未開始）

對照訪談 Q4 的決定，需要使用者親自完成的前置作業：

- 建立 Supabase（或同等）雲端服務帳號，把三組 API 金鑰改成存在伺服器端，而不是每次貼在瀏覽器。
- 申請 OpenAI Platform 帳號（使用者已表示「不是問題」，待實際申請）。

技術方向：

- 把金鑰管理與 API 呼叫邏輯搬到 Supabase Edge Function（或 `雲端函式` 資料夾裡已經起頭的 Cloudflare Worker），讓背景執行不需要使用者的瀏覽器分頁保持開啟。
- 串接各供應商的真實用量 API，讓費用預估變成真正的用量記帳，取代目前文字長度估算。
- 讓「啟動全隊」的每個階段可以各自指定供應商／模型，而不是整次團隊任務共用同一個。
- 改動前後的視覺化 diff／一鍵核准介面（訪談中列為「眼睛一亮」功能之一），取代目前純文字的階段結果訊息。
- 把「品保通過」做成任務往下一步走之前的硬性關卡，銜接 `decisions/log.md`（使用者 Claude Project）裡 2026-09-11 卡住的除錯／版本追蹤 level-up。

## 下次要做這個平台的開發時

因為 Phase 0 已經把「多供應商、分階段確認、品保可跳過、費用預估、共用錯誤記憶」這些基礎都搭好了，理論上從下一個任務開始，就可以嘗試用平台自己的「啟動全隊」機制（先從單一小任務、不略過品保開始）來處理後續的小改動，逐步驗證「平台管理自己開發」是否真的可行，再決定要不要把台股工具或治具工具的日常維護也交給它。

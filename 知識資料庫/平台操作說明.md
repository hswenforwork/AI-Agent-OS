# 平台操作說明

## 一、打開管理平台

使用瀏覽器開啟 GitHub Pages 網址。左側有五個功能：團隊畫布、AI 工作台、文件資料庫、任務進度、工作紀錄。

## 二、查看 AI 代理

在「團隊畫布」中，每張卡片代表一位 AI 代理。卡片會顯示代理名稱、角色、狀態、目前任務及完成進度。代理卡片可以拖曳，位置會保存在目前使用的瀏覽器。

## 三、AI 工作台：選模型、對話、啟動全隊

1. 左側選單切到「AI 工作台」。
2. 「AI 代理」選要對話的代理；「AI 模型」選你要用哪家供應商的哪個模型（Gemini／OpenAI／Claude，每次都可以換）。
3. 依選到的供應商，在對應欄位貼上金鑰（三家分開輸入，只會顯示目前選到那家的欄位）。沒有金鑰請看 [多模型 API 串接說明](多模型API串接說明.md)。
4. 「測試連線」確認金鑰可用。
5. 想交給單一代理，輸入內容後按「交給此代理」。
6. 想啟動整個團隊依序協作，輸入共同任務後按「啟動全隊」。**平台採分階段確認制**：每完成一個階段會停下來，顯示這一階段的結果與預估花費，按鈕會變成「核准並繼續（第 N/6 階段）」，你確認沒問題再按一次才會進到下一階段；隨時可以按「停止」中止。
7. 如果想省成本、不需要交叉檢查，可以勾選「啟動全隊時略過品保檢查」，團隊流程就會跳過檢查代理那一階段。
8. 「AI 工作台」左側的「本月費用預估」會顯示這個月大概花了多少、佔你設定的月預算多少百分比（可自行修改月預算數字）。這是粗略估算，不是供應商官方帳單。

## 四、更新代理資料

1. 在 GitHub 儲存庫開啟 `系統設定` 資料夾。
2. 開啟 `AI代理資料.json`。
3. 按右上角的鉛筆圖示。
4. 修改文字、完成進度或最後更新時間。
5. 按「Commit changes」儲存。

完成進度請輸入 0 到 100 的數字，不需要加百分比符號。每個逗號與雙引號都必須保留。

## 五、新增代理

複製一整段現有代理資料，貼在上一位代理資料後方，再修改名稱、角色與任務。兩段代理資料中間必須有逗號。

## 六、閱讀文件

進入「文件資料庫」，按文件卡片下方的「開啟文件」，即可在 GitHub 閱讀對應的繁體中文 Markdown 文件，包含所有代理共用的「已知問題與規則」——這是解決「改壞了不知道」這個問題的機制，值得養成先看一眼的習慣。

## 七、常見問題

### 顯示「尚無資訊」

代表該代理缺少對應欄位。請確認欄位名稱是否正確：`名稱`、`角色`、`狀態`、`任務`、`完成進度`、`最後更新`。

### 網頁沒有立即更新

GitHub Pages 發布通常需要一些時間。稍候數分鐘後重新整理瀏覽器。

### 卡片位置想恢復預設值

清除這個網站的瀏覽器儲存資料，再重新整理頁面。
